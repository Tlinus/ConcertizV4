<h1> edit </h1>

<%= form_for :genre, url: genre_path(@genre), method: :patch do |c| %>
	
	<p>
		<%= c.label :nom %><br>
		<%= c.text_field :nom %>
	</p>
	<p>
		<%= c.submit %>
	</p>
	
<% end %>

<%= link_to 'Back', concerts_path %>
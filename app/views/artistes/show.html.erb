<h1> show artiste</h1>

<p>
	<strong> Nom: </strong>
	<%= @artiste.nom %>
</p>
<p>
	<strong>Ses prochaines dates: </strong>

<% seances = Seance.where(artiste_id: @artiste.id) %>

<% seances.each do |s| %>
	<% concert  = Concert.find(s.concert_id) %>

	<%= link_to "#{concert.name}/#{s.date}", concerts_path(concert) %>

<% end %>
<% unless seances.count > 0 %>
    <%= "Cet artiste n'a pas de date prévue prochainement" %>
<% end %>
<br>
<%= link_to 'Accueil', concerts_path %>